set_io clk 35       // Clock signal on pin 35 (12MHz onboard clock)
set_io trigger 2    // Trigger signal on pin 2 (for HC-SR04)
set_io echo 3       // Echo signal on pin 3 (from HC-SR04)
set_io led 41       // LED output on pin 41 (blinks based on distance)
